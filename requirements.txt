# ============================================================================
# TJT — Autonomous Web3 Info-Broker :: Requirements Registry
# Hard-pinned versions to guarantee a reproducible, conflict-free environment.
# Target runtime: Python 3.11+ (validated on macOS / CPython 3.13)
# ============================================================================

# --- Frontend / UI workspace -------------------------------------------------
streamlit==1.39.0

# --- Data & numerical layer (financial calculators, market table) ------------
pandas==2.2.3
numpy==1.26.4

# --- HTTP clients (CoinGecko streaming monitor + self-healing fallback) ------
requests==2.32.3
httpx==0.27.2

# --- Multi-agent orchestration core (CrewAI engine) --------------------------
crewai==0.80.0
crewai-tools==0.14.0

# --- Language models / LLM SDK (FinOps tiered gpt-4o / gpt-4o-mini) ----------
# crewai ships its own litellm-backed LLM wrapper; the openai SDK is kept for
# any direct calls and to pin a known-good transitive version.
openai==1.54.4

# --- Environment & config handling -------------------------------------------
python-dotenv==1.0.1
filelock==3.16.1
